function verificarMaioridade(idade) {
    return idade >= 18 ? "Maior de idade" : "Menor de idade";
  }
  
  // Exemplo de uso:
  console.log(verificarMaioridade(17)); // Saída: Menor de idade
  console.log(verificarMaioridade(20)); // Saída: Maior de idade