function compararNumeros(num1, num2) {
  return num1 === num2 ? "São iguais" : "Não são iguais";
}

// Exemplo de uso:
console.log(compararNumeros(5, 5));  // Saída: São iguais
console.log(compararNumeros(3, 7));  // Saída: Não são iguais